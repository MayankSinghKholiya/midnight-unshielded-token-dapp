console.log(`\nPreprod deployment note\n\nThe Preprod deploy path needs a funded deploy wallet, tDUST, local proof server, and a Midnight.js adapter compatible with the currently published wallet SDK versions.\n\nThis final zip intentionally keeps the unstable experimental deploy code out of the runnable scripts so your install/build flow stays clean. Use docs/LOCAL_DOCKER_DEPLOYMENT.md and docs/SUBMISSION_CHECKLIST.md for the bounty-ready deployment checklist.\n`);
