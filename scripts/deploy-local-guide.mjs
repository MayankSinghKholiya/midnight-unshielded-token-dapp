console.log(`\nLocal Docker deployment guide\n\nThis repository includes the Compact contract, generated artifacts, React UI, and a local API adapter.\n\nFor a true Midnight local-chain deployment, start the official midnight-local-dev stack separately, then wire a Midnight.js adapter to the endpoints described in docs/LOCAL_DOCKER_DEPLOYMENT.md.\n\nThe stable run path included in this zip is:\n\n  npm run setup\n  npm run check:artifacts\n  npm run api:dev\n  cd frontend && cp .env.example .env && npm run dev\n\nThis command exits successfully so it does not break your setup. Read docs/LOCAL_DOCKER_DEPLOYMENT.md before attempting live deployment.\n`);
