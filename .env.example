# Optional: when a server-side Midnight contract adapter is available, point the frontend at it.
# Without this value the UI runs in local demo mode so reviewers can inspect UX immediately.
VITE_CONTRACT_API_URL=http://localhost:4000/api
